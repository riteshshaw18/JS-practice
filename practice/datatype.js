"use strict";//treats all js code in newer version.
//alert(3+3)
//it won't work here because we are using node here ,it works on browser.
let age=89;//it is a type of number datatype with range of 2^53
let name="Ritesh";//datatype string,we can use single inverted comma too but 
//for good practice we use double inverted comma
let isFemale=false;//boolean 
let wife;//undefined
let job=null;//null represents absence of value.(eg: we ask temp from server ,but due to 
//any issue we cant et it so we tell that ok dont gve me 0 because it is also a temp ,give me null)

typeof(78);//it tell us type of entered value.

symbol //unique.


//object (one of eg is null)
